export default {
  /**
   * 默认菜单状态(shrink收缩、expand展开)
   */
  menuStatus: 'expand',
  /**
   * 默认主题(light亮色、dark暗色)
   */
  theme: 'light',
  /**
   * 默认语言(zh-CN简体中文、zh-TW繁体中文、en-US英文)
   */
  lang: 'zh-CN',
  /**
   * 默认组件大小(large、default、small)
   */
  size: 'default'
}